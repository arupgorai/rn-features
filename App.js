import React from 'react';
import {SafeAreaView, ScrollView, StyleSheet, View, Text} from 'react-native';
import ProgressOnScroll from './animation/ProgressOnScroll';

const App = () => {
  return (
    <SafeAreaView>
      <ProgressOnScroll />
    </SafeAreaView>
  );
};

export default App;
